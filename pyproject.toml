[project]
name = "ai-debug-assistant"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[all]",
    "uvicorn",
    "pydantic",
    "python-dotenv",
    "requests",
]

[tool.vercel]
entrypoint = "ai_debug_assistant.main:app"
